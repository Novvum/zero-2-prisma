# Migration `20190619062514-init`

This migration has been generated by rajinwonderland at 6/19/2019, 6:25:14 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:dev"."Character"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"marvelId" TEXT NOT NULL DEFAULT '' ,"name" TEXT NOT NULL DEFAULT '' ,"description" TEXT   ,"thumbnail" TEXT   ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Movie"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"film" TEXT NOT NULL DEFAULT '' ,"releaseYear" INTEGER   ,"budget" INTEGER   ,"runningTime" INTEGER   ,"domesticGross" INTEGER   ,"weekendGross" INTEGER   ,"overseasGross" INTEGER   ,"worldwideGross" INTEGER   ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."_CharacterToMovie"("A" TEXT NOT NULL  REFERENCES Character(id),"B" TEXT NOT NULL  REFERENCES Movie(id));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190619062514-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,37 @@
+datasource db {
+    provider = "sqlite"
+    url      = "file:dev.db"
+    default  = true
+}
+
+generator photon {
+    provider = "photonjs"
+    output   = "node_modules/@generated/photon"
+}
+
+model Character {
+    id String @default(cuid()) @id @unique
+    createdAt DateTime @default(now())
+    updatedAt DateTime @updatedAt
+    marvelId String
+    name String
+    description String?
+    thumbnail String?
+    movies Movie[]
+}
+
+model Movie {
+    id String @default(cuid()) @id @unique
+    createdAt DateTime @default(now())
+    updatedAt DateTime @updatedAt
+    film String
+    releaseYear Int?
+    budget Int?
+    runningTime Int?
+    domesticGross Int?
+    weekendGross Int?
+    overseasGross Int?
+    worldwideGross Int?
+    characters Character[]
+}
+
```

## Photon Usage

You can use a specific Photon built for this migration (20190619062514-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190619062514-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
