# Migration `20190619064833-init`

This migration has been generated by rajinwonderland at 6/19/2019, 6:48:33 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Movie"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"film" TEXT NOT NULL DEFAULT '' ,"releaseYear" INTEGER   ,"budget" REAL   ,"runningTime" INTEGER   ,"domesticGross" INTEGER   ,"weekendGross" INTEGER   ,"overseasGross" INTEGER   ,"worldwideGross" INTEGER   ,PRIMARY KEY ("id"));

INSERT INTO "new_Movie" ("id","createdAt","updatedAt","film","releaseYear","budget","runningTime","domesticGross","weekendGross","overseasGross","worldwideGross") SELECT "id","createdAt","updatedAt","film","releaseYear","budget","runningTime","domesticGross","weekendGross","overseasGross","worldwideGross" from "Movie"

DROP TABLE "lift"."Movie";

ALTER TABLE "lift"."new_Movie" RENAME TO "Movie";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190619062514-init..20190619064833-init
--- datamodel.dml
+++ datamodel.dml
@@ -25,9 +25,9 @@
     createdAt DateTime @default(now())
     updatedAt DateTime @updatedAt
     film String
     releaseYear Int?
-    budget Int?
+    budget Float?
     runningTime Int?
     domesticGross Int?
     weekendGross Int?
     overseasGross Int?
```

## Photon Usage

You can use a specific Photon built for this migration (20190619064833-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190619064833-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
