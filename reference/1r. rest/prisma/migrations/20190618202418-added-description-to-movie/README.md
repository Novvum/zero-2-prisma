# Migration `20190618202418-added-description-to-movie`

This migration has been generated by Rohit at 6/18/2019, 8:24:18 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "lift"."Movie" ADD COLUMN "description" TEXT   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190618201118-changed-to-superheroes..20190618202418-added-description-to-movie
--- datamodel.dml
+++ datamodel.dml
@@ -16,11 +16,12 @@
     movies Movie[]
 }
 model Movie {
-    id        String   @default(cuid()) @id @unique
-    createdAt DateTime @default(now())
-    updatedAt DateTime @updatedAt
-    released  Boolean
-    title     String
-    mainHero  Hero?
+    id           String   @default(cuid()) @id @unique
+    createdAt    DateTime @default(now())
+    updatedAt    DateTime @updatedAt
+    released     Boolean
+    title        String
+    description  String?
+    mainHero     Hero?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190618202418-added-description-to-movie)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190618202418-added-description-to-movie'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
